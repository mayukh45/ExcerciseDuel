# Copy to .env after running infra/deploy.sh. Both are printed by the deploy script.
# EXPO_PUBLIC_* vars are inlined into the client bundle at build time.
# If left unset, the app runs local-only (single device, no cross-phone sync).
EXPO_PUBLIC_API_BASE=
EXPO_PUBLIC_API_KEY=
